import type { NextRequest } from "next/server";

export function middleware(_req: NextRequest) {
  // Placeholder for future (auth, redirects, etc.)
  return;
}

export const config = {
  matcher: []
};
